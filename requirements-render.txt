# Render (Linux) install: app deps + CPU Torch + SpeechBrain LID.
--extra-index-url https://download.pytorch.org/whl/cpu
-r requirements.txt
torch==2.5.1+cpu
torchaudio==2.5.1+cpu
speechbrain>=1.0.0,<2
huggingface_hub>=0.23.0
