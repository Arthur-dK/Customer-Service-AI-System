# Render (Linux) install: app deps + CPU Torch + SpeechBrain LID + Whisper + BGE-M3.
--extra-index-url https://download.pytorch.org/whl/cpu
-r requirements.txt
-r requirements-ivr-intent.txt
torch==2.9.1+cpu
torchaudio==2.9.1+cpu
speechbrain>=1.0.0,<2
huggingface_hub>=0.23.0
scipy>=1.11.0
